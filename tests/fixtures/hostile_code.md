# Hostile inline code

Inline code reaches the PDF verbatim, so a `` `backtick` ``, a `#hash`, a
`$dollar`, and a `\backslash` all survive.

All four at once: `` #hash $dollar \backslash `backtick` ``.
