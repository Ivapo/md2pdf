[workspace]
members = ["core", "cli"]
resolver = "3"

[workspace.package]
version = "0.1.2"
edition = "2024"
license = "MIT"

# The embedded Typst compiler dominates the binary size. These settings cut
# the release binary roughly in half; they change nothing about behaviour.
[profile.release]
strip = true
lto = "thin"
codegen-units = 1
